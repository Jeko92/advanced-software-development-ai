PORT='<Declare app port here>'
DB_FILE='<Declare sqlite database file path here, e.g. data/cyber-chat.sqlite>'
JWT_SECRET='<64-char hex string, e.g. openssl rand -hex 32>'
BCRYPT_SALT_ROUNDS='<Optional. bcrypt cost factor for hashing passwords. Defaults to 12 if unset.>'
