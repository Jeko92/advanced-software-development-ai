meta {
  name: Register Intruder
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "chat_intruder",
    "password": "hunter2ButBetter"
  }
}

assert {
  res.status: eq 201
}

script:post-response {
  bru.setVar("intruderUserId", res.body.id);
}
