meta {
  name: Update Thread (Forbidden)
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/threads/{{threadId}}
  body: json
  auth: bearer
}

body:json {
  {
    "title": "Hijacked by an intruder"
  }
}

assert {
  res.status: eq 403
}

auth:bearer {
  token: {{intruderToken}}
}
