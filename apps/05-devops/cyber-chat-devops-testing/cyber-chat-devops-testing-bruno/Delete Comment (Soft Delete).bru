meta {
  name: Delete Comment (Soft Delete)
  type: http
  seq: 19
}

delete {
  url: {{baseUrl}}/comments/{{commentId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 204
}

auth:bearer {
  token: {{accessToken}}
}
