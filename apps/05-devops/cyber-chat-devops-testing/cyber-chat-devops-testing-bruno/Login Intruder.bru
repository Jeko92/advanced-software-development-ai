meta {
  name: Login Intruder
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "chat_intruder",
    "password": "hunter2ButBetter"
  }
}

assert {
  res.status: eq 201
  res.body.access_token: isDefined
}

script:post-response {
  bru.setVar("intruderToken", res.body.access_token);
}
