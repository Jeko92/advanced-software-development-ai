meta {
  name: Get Comment By Id
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/comments/{{commentId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 200
}

auth:bearer {
  token: {{accessToken}}
}
