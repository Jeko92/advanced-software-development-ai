meta {
  name: Get User By Id
  type: http
  seq: 26
}

get {
  url: {{baseUrl}}/users/{{userId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 200
  res.body.passwordHash: isUndefined
}

auth:bearer {
  token: {{accessToken}}
}
