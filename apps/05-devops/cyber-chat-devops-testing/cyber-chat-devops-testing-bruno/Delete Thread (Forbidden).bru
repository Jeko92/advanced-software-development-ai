meta {
  name: Delete Thread (Forbidden)
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/threads/{{threadId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 403
}

auth:bearer {
  token: {{intruderToken}}
}
