meta {
  name: Get Thread By Id
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/threads/{{threadId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 200
}

auth:bearer {
  token: {{accessToken}}
}
