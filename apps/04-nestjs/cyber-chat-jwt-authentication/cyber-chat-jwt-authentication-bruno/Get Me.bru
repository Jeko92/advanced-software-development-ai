meta {
  name: Get Me
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/auth/me
  body: none
  auth: bearer
}

assert {
  res.status: eq 200
  res.body.username: eq chat_wanderer
  res.body.passwordHash: isUndefined
}

auth:bearer {
  token: {{accessToken}}
}
