meta {
  name: Delete Intruder
  type: http
  seq: 33
}

delete {
  url: {{baseUrl}}/users/{{intruderUserId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 204
}

auth:bearer {
  token: {{intruderToken}}
}
