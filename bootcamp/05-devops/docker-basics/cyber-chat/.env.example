DATABASE_PATH='<Path to the SQLite database file, e.g. db/cyber-chat.db>'
# 64-char hex string used to sign/verify JWTs. Generate one with:
#   openssl rand -hex 32
JWT_SECRET='<64-char hex string, e.g. openssl rand -hex 32>'
