meta {
  name: Delete Thread
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/threads/{{threadId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 204
}

auth:bearer {
  token: {{accessToken}}
}
