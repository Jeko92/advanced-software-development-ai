meta {
  name: Get Thread By Id (Invalid Id)
  type: http
  seq: 22
}

get {
  url: {{baseUrl}}/threads/not-a-uuid
  body: none
  auth: bearer
}

assert {
  res.status: eq 400
}

auth:bearer {
  token: {{accessToken}}
}
