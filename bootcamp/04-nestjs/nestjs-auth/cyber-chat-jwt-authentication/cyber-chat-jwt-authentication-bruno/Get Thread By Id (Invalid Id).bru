meta {
  name: Get Thread By Id (Invalid Id)
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/threads/not-a-uuid
  body: none
  auth: bearer
}

assert {
  res.status: eq 400
}

auth:bearer {
  token: {{accessToken}}
}
