meta {
  name: Get Comment By Id (Invalid Id)
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/comments/not-a-uuid
  body: none
  auth: bearer
}

assert {
  res.status: eq 400
}

auth:bearer {
  token: {{accessToken}}
}
