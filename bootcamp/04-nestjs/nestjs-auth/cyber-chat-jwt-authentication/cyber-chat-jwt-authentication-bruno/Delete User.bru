meta {
  name: Delete User
  type: http
  seq: 22
}

delete {
  url: {{baseUrl}}/users/{{userId}}
  body: none
  auth: bearer
}

assert {
  res.status: eq 204
}

auth:bearer {
  token: {{accessToken}}
}
