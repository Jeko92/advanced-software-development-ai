meta {
  name: Delete Api Key
  type: http
  seq: 38
}

delete {
  url: {{baseUrl}}/api-keys/{{apiKeyId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}
