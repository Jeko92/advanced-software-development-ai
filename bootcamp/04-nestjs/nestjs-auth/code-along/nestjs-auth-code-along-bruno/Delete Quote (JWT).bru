meta {
  name: Delete Quote (JWT)
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/quotes/{{quoteId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

assert {
  res.status: eq 200
}
